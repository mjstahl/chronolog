<!DOCTYPE html>
<html>

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Mark Stahl - {{ formattedDate }}
  </title>
  <link rel="icon" type="image/png" href="https://avatars2.githubusercontent.com/u/212726?s=32">
  <link href="https://fonts.googleapis.com/css2?family=Libre+Baskerville&family=Rubik:wght@300&display=swap"
    rel="stylesheet">
  <link rel="stylesheet" href="/styles/styles.css">
</head>

<body>
  <header>
    <div class="name">
      <a href="/">mark stahl</a>
    </div>
    <div class="topics">
      <a href="/">days</a>
    </div>
  </header>
  <main>
    <article>
      <header>
        <h1>{{ formattedDate }}</h1>
      </header>
      <div class="body">
        {{#each sections}}
          <div class="section">
            <time>
              {{ time }}
            </time>
            {{#each content as |post|}}
              {{#ifEquals post.type "media"}}
                <img src="{{ post.value }}" />
              {{else}}
                <p>{{ post.value }}</p>
              {{/ifEquals}}
            {{/each}}
          </div>
        {{/each}}
      </div>
    </article>
  </main>
  <footer>
    &copy; {{ year }}
  </footer>
</body>

</html>